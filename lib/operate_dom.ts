import { Slide, Parameters } from './index';

export function init_default_style (context:Slide) : void {
  
}

export type pointTouchArea = Parameters['point_touch_area']
export function expand_touch (context:Slide, el:HTMLElement, point_touch_area:pointTouchArea) : void {

}